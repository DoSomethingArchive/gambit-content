'use strict';

const configVars = {};

configVars.containerProperty = 'body';
configVars.incomingMessageTypes = {
  keyword: 'incoming keyword',
  args: 'incoming input',
  mms_image_url: 'incoming picture url',
};
module.exports = configVars;
